import { LightningElement, wire } from 'lwc';
// import viewProducts from '@salesforce/apex/DisplayProducts.viewProducts';

export default class listproducts extends LightningElement{
    //@wire(viewProducts) products;
    //error;
    /*
    connectedCallback(){
        this.loadProducts();
    }

    loadProducts() {
        viewProducts()
            .then(result => {
                this.products = result;

            })
            .catch(error =>{
                this.error = error;
            })

    }
    */
}